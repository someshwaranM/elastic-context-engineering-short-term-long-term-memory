# ML/AI packages (CPU-only versions) - Optional, only needed for Provence reranker
# Install using: pip install -r requirements-ml-cpu.txt

--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0
--extra-index-url https://pypi.org/simple
transformers>=4.30.0,<5.0.0

